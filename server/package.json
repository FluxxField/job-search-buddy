{
  "name": "server",
  "version": "1.0.0",
  "description": "Dependencies for the server side of the app",
  "main": "server.js",
  "scripts": {
    "start": "set DEBUG=* & nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "debug": "^4.1.1",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
